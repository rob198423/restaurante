# ToneMind AI audio layer

This directory contains standalone AI/audio building blocks that can be called by the FastAPI service as the MVP grows.

## Planned modules

- `librosa_key_bpm.py`: key and tempo extraction.
- `demucs_separation.py`: instrument separation wrapper.
- `midi_generation.py`: MIDI export helpers.
